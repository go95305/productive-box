name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.db5872222341e634c76d3cad77baabe828db36b1 }}
          GIST_ID: ${{ secrets.56a3dbf0b5ea7535fd2a2bbbcf099d84 }}
          TIMEZONE: Asia/Seoul
